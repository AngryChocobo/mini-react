import { expect, test } from "vitest";
import { React } from "./react";
test("Happy path", () => {
    expect(React).toBeTruthy();
    const msg = "Hello World and ";
    const App = (draggable >
        className) = "h2-hello" > { msg }, Hello, React;
    /h2>
        < p > I;
    am;
    a;
    pargraph < /p>
        < input;
    type = "text" /  >
        /div>;
});
React.render(App, document.getElementById("app"));
;
